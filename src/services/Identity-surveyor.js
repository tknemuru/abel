'use strict'

module.exports = {
  /**
   * @description 素性の調査を行います。
   * @returns {void}
   */
  survey () {
    // 調査対象のカラムを取得
    // テンプレートのSQLを取得
    // テンプレートからSQLを組み立てる
    // パラメータを作成
    // SQLを実行
    return null
  }
}
